<!-- $("セレクタ")の部分はどこを変えるかという記述 -->
<!-- .html();の部分は、どう変えるかを指定している -->
<!-- .html()で先ほど指定した部分の中身を変えることを宣言している -->
<!-- j()は部分テンプレートをjsファイルに読み込む際に使うもの -->
$("#book_<%= @book.id %>").html("<%= j(render "favorites/btn", book: @book) %>");